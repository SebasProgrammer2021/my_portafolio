version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install --cache .npm --prefer-offline
    build:
      commands:
        - yarn run build
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - .next/cache/**/*
      - .npm/**/*
